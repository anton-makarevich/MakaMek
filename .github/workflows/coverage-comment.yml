name: Coverage Comments
permissions:
  contents: read
on:
  workflow_run:
    workflows:
      - MakaMek Core Library
      - MakaMek Presentation Library
      - MakaMek Bots Library
      - MakaMek Avalonia Library
      - MakaMek Assets Library
      - MakaMek Localization Library
      - MakaMek Map Library
    types:
      - completed

jobs:
  post-coverage-comment:
    runs-on: ubuntu-latest
    permissions:
      actions: read
      pull-requests: write
    if: >
      github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.event == 'pull_request'
    steps:
      - uses: actions/download-artifact@v4
        id: download
        continue-on-error: true
        with:
          pattern: cocodif-*
          path: coverage
          merge-multiple: true
          github-token: ${{ secrets.GITHUB_TOKEN }}
          run-id: ${{ github.event.workflow_run.id }}

      - name: Validate report and resolve PR number
        id: resolve
        if: steps.download.outcome == 'success'
        shell: bash
        run: |
          test -f coverage/report.md
          PR_NUMBER="${{ github.event.workflow_run.pull_requests[0].number }}"
          if [ -z "$PR_NUMBER" ]; then
            PR_NUMBER=$(gh api "repos/${{ github.repository }}/actions/runs/${{ github.event.workflow_run.id }}" --jq '.pull_requests[0].number // empty' 2>/dev/null || true)
          fi
          if [ -z "$PR_NUMBER" ]; then
            echo "::error::Could not determine pull request number from workflow_run event or GitHub API"
            exit 1
          fi
          echo "pr_number=$PR_NUMBER" >> "$GITHUB_OUTPUT"

      - name: Post coverage comment
        if: steps.resolve.outcome == 'success'
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          number: ${{ steps.resolve.outputs.pr_number }}
          header: ${{ github.event.workflow_run.name }}
          path: coverage/report.md
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}