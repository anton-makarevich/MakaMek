name: Hub Infra (Pulumi)

# Ad-hoc deployment of the MakaMek relay hub OCI infrastructure.
# No automatic triggers: run manually from the Actions tab.

on:
  workflow_dispatch:
    inputs:
      command:
        description: "Pulumi command"
        type: choice
        required: true
        default: preview
        options:
          - preview
          - up
          - destroy
      stack:
        description: "Pulumi stack name"
        type: string
        required: true
        default: prod
      imageTag:
        description: "Hub image tag (optional, overrides stack config)"
        type: string
        required: false
      adIndex:
        description: "Availability domain index 0-based (optional, overrides stack config)"
        type: string
        required: false

permissions:
  contents: read

jobs:
  pulumi:
    name: pulumi ${{ inputs.command }}
    runs-on: ubuntu-latest
    # Protected environment: required reviewers gate every run of this job
    # (including destroy), so arbitrary dispatch inputs cannot mutate infra
    # without manual approval.
    environment: makamek-hub-infra
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-dotnet@v6
        with:
          dotnet-version: 10.0.x

      # Restore Pulumi config (incl. secrets) from the cloud backend and pass
      # an optional imageTag override without touching stored config.
      # Build per-run config overrides (never persisted to stack config).
      - name: Build Pulumi config overrides
        id: cfg
        shell: bash
        run: |
          map=$(jq -n '{}')
          if [ -n "$IMAGE_TAG" ]; then
            map=$(jq --arg v "$IMAGE_TAG" '. + {imageTag: {value: $v, secret: false}}' <<<"$map")
          fi
          if [ -n "$AD_INDEX" ]; then
            map=$(jq --argjson v "$AD_INDEX" '. + {availabilityDomainIndex: {value: $v, secret: false}}' <<<"$map")
          fi
          echo "map=$map" >> "$GITHUB_OUTPUT"
        env:
          IMAGE_TAG: ${{ inputs.imageTag }}
          AD_INDEX: ${{ inputs.adIndex }}

      - uses: pulumi/actions@v7
        with:
          command: ${{ inputs.command }}
          stack-name: ${{ github.repository_owner }}/makamek-hub-infra/${{ inputs.stack }}
          work-dir: src/MakaMek.Hub.Infra
          upsert: false
          refresh: true
          config-map: ${{ steps.cfg.outputs.map }}
          expect-no-changes: false
        env:
          PULUMI_ACCESS_TOKEN: ${{ secrets.PULUMI_ACCESS_TOKEN }}
          OCI_TENANCY_OCID: ${{ secrets.OCI_TENANCY_OCID }}
          OCI_USER_OCID: ${{ secrets.OCI_USER_OCID }}
          OCI_FINGERPRINT: ${{ secrets.OCI_FINGERPRINT }}
          OCI_PRIVATE_KEY: ${{ secrets.OCI_PRIVATE_KEY }}
          OCI_REGION: ${{ secrets.OCI_REGION }}
